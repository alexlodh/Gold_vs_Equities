version: "3.8"
services:
  web:
    build:
      # Build from repository root so we can COPY data/ into the image during build
      context: .
      dockerfile: web/Dockerfile
    ports:
      - "3001:3000" # host:container (serve Next.js on host 3001)
    environment:
      - PORT=3000
    restart: unless-stopped
